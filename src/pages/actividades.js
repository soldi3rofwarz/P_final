import React from 'react';
import {Layout} from './../elements/theme/components/layout';
import {ListContainer} from './../elements/actividades/containers/list-container';

export const ActividadesPage = () => {
    return (
        <Layout>
            <ListContainer />
        </Layout>
    );
};